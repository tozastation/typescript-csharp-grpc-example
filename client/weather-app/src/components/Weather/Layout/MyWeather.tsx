import * as React from 'react';
import WeatherProps from '../Data/WeatherProps';

export default class MyWeather extends React.Component<WeatherProps, any> {
    constructor(props: WeatherProps) {
        super(props);
    }
    public render() {
        return (
          <div>
            
          </div>
        );
      } 
}